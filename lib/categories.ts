/** Canonical entry categories — enforced on all submission endpoints. */
export const VALID_CATEGORIES: string[] = [
  'Earn Now',
  'Platforms',
  'Infrastructure',
  'Token Agents',
];

export type EntryCategory = 'Earn Now' | 'Platforms' | 'Infrastructure' | 'Token Agents';
